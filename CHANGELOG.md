# 변경 내역

## 0.2.0

- **문제 우클릭 → 풀이 파일 만들기** — 문제 이름으로 파일을 만들고 바로 엽니다 (`CF 1850A` → `CF_1850A.cpp`). 기본 확장자는 `cpp`, `cplog.fileExtension`으로 바꿉니다
- `cplog.fileDirectory` 설정 — 풀이 파일을 만들 폴더(워크스페이스 루트 기준 상대 경로). 비우면 루트, 폴더가 없으면 만듭니다
- 같은 이름의 파일이 이미 있으면 **덮어쓰지 않고 열기만** 합니다 — 풀던 코드가 날아가지 않습니다
- 파일명의 **공백은 밑줄(`_`)로** 바꿉니다 — 셸에서 따옴표 없이 컴파일·실행할 수 있게. 파일명에 못 쓰는 글자(`: / \ * ? " < > |`, 제어문자)도 밑줄이 되고, 끝의 점·공백과 Windows 예약 장치 이름(`CON` 등)도 처리합니다
- **`CP-Log: 문제 이름 복사`를 없앴습니다** — 우클릭 자리를 파일 만들기가 대신합니다
- 새로고침이 도는 중에 초기 설정이 끝나면 토큰 없이 요청이 나가 엉뚱한 "GitHub 토큰이 없습니다" 오류가 뜨던 문제를 고쳤습니다

## 0.1.2

- **아이콘 교체** — 액티비티 바 아이콘과 마켓플레이스 아이콘이 GitHub 옥토캣이었습니다. 익스텐션의 상태 표기(해결 `✓` / 미해결 `○`)를 그대로 옮긴 체크리스트 아이콘으로 바꿨습니다. 남의 로고를 제품 아이콘으로 쓰면 GitHub 로고 정책에 어긋나고 공식 확장으로 오해될 소지도 있습니다
- 마켓플레이스용 `icon`(256×256 PNG)과 `galleryBanner` 지정 — 이전에는 최상위 `icon` 필드가 없어 회색 기본 타일로 보였습니다

## 0.1.1

문서만 바뀌었습니다. 익스텐션 동작은 0.1.0과 같습니다.

- README에 사이드바 미리보기 추가 — 진행도가 숨긴 해결 문제까지 센다는 점, 전부 해결한 그룹이 목록에서 사라진다는 점을 예시로 설명
- 요구 사항 섹션 추가 (VS Code 1.85.0 이상, 앱의 데이터 저장소 동기화 전제)
- 읽기 전용 PAT 발급 절차를 단계별로 안내
- 명령 6종과 실행 위치를 표로 정리
- 문제 해결 섹션 추가 — 실제 오류 메시지를 원인·해결과 연결
- MIT LICENSE 전문 추가 (`package.json`이 선언만 하고 파일이 없었음)

## 0.1.0

첫 릴리스.

- 문제집(그룹)별 접이식 트리 — 그룹마다 `3/8 해결` 진행도, 전부 해결한 그룹은 숨김
- CP-Log 웹앱과 동일한 정렬 — 미해결 우선, 같은 상태 안에서는 최신 등록순
- 문제 클릭 시 브라우저로 열기, 우클릭으로 이름 복사
- 복습 기한이 도래한 문제를 노란 벨 아이콘으로 강조
- 상태바에 미해결 개수와 복습 대기 개수
- 자동 새로고침 (기본 10분, `0`이면 수동만)
- 마지막 목록을 캐시해 시작 직후 네트워크 응답 전에도 표시
- 토큰은 VS Code SecretStorage에만 저장
